import React from 'react';
import ReactDOM from 'react-dom/client';
import WilfriedPortfolio from './wilfried-portfolio';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WilfriedPortfolio />
  </React.StrictMode>
);
